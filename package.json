{
  "name": "rope_av_js",
  "version": "1.0.0",
  "description": "Rope algorithm visualizer",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "start": "parcel index.html -p 3000",
    "build": "rm -rf dist && parcel build index.html --dist-dir dist --public-url /bloom_filter_algorithm_visualizer/",
    "predeploy": "rm -rf dist .parcel-cache; npm run build",
    "deploy": "git push origin --delete gh-pages; gh-pages -d dist ; rm -rf .parcel-cache dist",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.25.8",
    "assert": "^2.1.0",
    "babel-jest": "^29.7.0",
    "buffer": "^6.0.3",
    "constants-browserify": "^1.0.0",
    "eslint": "^10.0.2",
    "events": "^3.3.0",
    "gh-pages": "^6.3.0",
    "jest": "^29.7.0",
    "os-browserify": "^0.3.0",
    "path-browserify": "^1.0.1",
    "prettier": "^3.6.2",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0",
    "util": "^0.12.5"
  },
  "dependencies": {
    "@faker-js/faker": "^10.0.0",
    "i18next": "^25.5.2",
    "parcel": "^2.15.4"
  },
  "prettier": {
    "singleQuote": true,
    "tabWidth": 4
  }
}
